# Datos de tu base de datos en pgAdmin
spring.datasource.url=jdbc:postgresql://localhost:5433/bd_valle
spring.datasource.username=postgres
spring.datasource.password=valledelsol

# Esto crea las tablas solo por ti
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Puerto de salida
server.port=8080